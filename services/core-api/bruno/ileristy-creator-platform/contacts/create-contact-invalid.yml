info:
  name: Create Contact - Validation Error
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/contacts"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "firstName": "",
        "lastName": "Doe",
        "email": "not-an-email",
        "marketingConsent": false
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 400 problem detail", function () {
          expect(res.status).to.equal(400);
          expect(res.body.status).to.equal(400);
        });

settings:
  encodeUrl: true
